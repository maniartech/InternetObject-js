{
  "name": "multi-validation-error-one-item",
  "description": "One parsed record fails multiple validations but yields exactly one record-error item (first validation error).",
  "protocol": ["§7"],
  "definitions": null,
  "options": { "defaultSchema": null },
  "input": "~ $User: { name: string, age: int, active: bool }\n--- $User\n~ 123, \"old\", \"yes\"\n",
  "chunkings": ["whole", "per-line"],
  "expected": {
    "items": [
      { "kind": "record-error", "recordIndex": 0, "schemaName": "$User", "error": { "category": "validation", "code": "not-a-string" } }
    ],
    "fatal": null
  }
}
