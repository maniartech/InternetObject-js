// Utility functions for schema manipulation can be added here.
// Example: function to clone a schema, validate schema structure, etc.

export function cloneSchema(schema: any): any {
  // ...implementation...
  return JSON.parse(JSON.stringify(schema));
}
