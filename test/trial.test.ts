import 'jest'

import IO from '../src/'

/**
 * To run the test trialpad, run following commands.
 *
 * npm run test -- trial
 * yarn test -- trial
 */

describe('Test Pad', () => {
  it('helps you test anything quickly', () => {
    // Quickly test anything here, just don't push changes to the server!
  })
})
